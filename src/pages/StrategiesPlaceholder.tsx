import { ComingSoon } from './ComingSoon'

export function StrategiesPlaceholder() {
  return (
    <ComingSoon
      title="Strategies"
      description="Strategy builder, backtest runs, and the live-status validation gate (spec §6) land in a follow-up pass."
    />
  )
}
